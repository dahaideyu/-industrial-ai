# 唯一生效的 .dockerignore：build context 是仓库根目录（compose build context: ../..），
# Docker 只读取 context 根下的这一份。不要在 deploy/docker/ 下另建 .dockerignore（不生效）。

# Python
**/__pycache__
*.pyc
*.pyo
.venv/
venv/
env/

# Node.js（**/ 前缀才能匹配 frontend/node_modules 等非根级目录）
**/node_modules
frontend/dist/
frontend/.vite/
dist/
build/

# Git / IDE / 工具目录
.git/
.gitignore
.idea/
.vscode/
*.swp
.claude/
.codex/
.codegraph/

# Docker compose 文件（构建时不需要）
deploy/docker/docker-compose*.yml
deploy/docker/.env*

# 日志和数据（容器内通过 volume 挂载）
logs/
data/
*.log
analysis_output/

# 环境变量（运行时通过 env_file 注入）
.env
.env.*
!.env.example

# 测试
tests/

# 本地开发脚本（容器内不需要）
*.bat
*.ps1

# 文档和杂项
docs/
*.md
!README.md
*.xlsx
*.doc
*.docx
